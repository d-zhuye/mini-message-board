<!DOCTYPE html>
<html lang="en">
  <%- include("./partials/head") %>
  <body>
    <div class="messages-container">
      <% messages.forEach(message => { %> <%- include("./partials/messages",
      {message: message}); %> <% }); %>
    </div>
  </body>
</html>
